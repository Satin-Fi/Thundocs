import React from "react";
import { Zap } from "lucide-react";

export const ThundocsLogo = ({ className = "w-10 h-10" }: { className?: string }) => (
  <Zap className={className} fill="currentColor" />
);
